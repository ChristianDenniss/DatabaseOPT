/**
 * @deprecated Use `bench.catalog.ts` (`BENCH_APPROACHES`, `BENCH_GLOBAL_OPTIMIZATIONS`).
 * Kept so older imports of `BENCH_APPROACHES` still resolve.
 */
export { BENCH_APPROACHES, type BenchApproach } from "./bench.catalog.js";
